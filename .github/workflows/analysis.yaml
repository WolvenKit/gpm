name: static check
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
jobs:
  analysis:
    name: Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v1.2.1
